## 1. Governance Implementation

- [x] 1.1 Add the API response instant-field contract to the API design rules in `AGENTS.md`, including Unix timestamp milliseconds, DTO types, prohibited types, and calendar-date exceptions.
- [x] 1.2 Add the time-field manual review requirement to the RESTful/API DTO checklist in the `lina-review` skill.

## 2. Verification and Records

- [x] 2.1 Run `openspec validate api-timestamp-response-contract --strict` to validate this OpenSpec change.
- [x] 2.2 Record the impact assessment for `i18n`, cache consistency, data permissions, runtime APIs, frontend pages, and development scripts.

## Records

- 2026-05-18: Completed API response time-field contract governance. `AGENTS.md` now requires public HTTP JSON response DTO instant fields to return Unix timestamps in milliseconds, prohibits directly exposing `time.Time`/`gtime.Time` or formatted instant strings, requires `date-only` semantics for calendar-date fields, and requires separate compatibility evaluation for existing API migrations. `.agents/skills/lina-review/SKILL.md` now includes a RESTful/API DTO manual review requirement for response time fields. Verification passed: `openspec validate api-timestamp-response-contract --strict`. `i18n` impact: this change only adds governance rules and review requirements, and does not add or modify frontend runtime language packs, host/plugin `manifest/i18n`, or `apidoc i18n JSON` resources; future migrations of existing API time fields must separately evaluate `apidoc i18n`. Cache consistency impact: no runtime cache, invalidation, shared revision, or cross-instance coordination changes. Data-permission impact: no runtime data-operation APIs are added or modified. Runtime API impact: no actual API implementation or response fields are changed; this only defines the contract for future new or modified response DTOs. Frontend page impact: no page or formatting logic changes. Development script impact: no static scanning tool is added, and no development, build, or test scripts are changed.

## Feedback

- [x] **FB-1**: Implement the timestamp response contract in existing public API response DTOs instead of only documenting the rule.

## Feedback Records

- 2026-05-18: Completed FB-1 implementation. Existing host and source-plugin public response DTO instant fields now use Unix millisecond timestamp values (`int64`/`*int64`) at the HTTP JSON boundary, with internal `time.Time`, `*gtime.Time`, and legacy stored strings projected through `apps/lina-core/pkg/apitime`. Updated affected frontend API types and all reviewed page display points to use `formatTimestamp`, while keeping date-range query parameters such as `beginTime`, `endTime`, `createdAt` RangePicker state, `loginTime` RangePicker state, and `operTime` RangePicker state as strings. Updated host and plugin `zh-CN` `apidoc i18n JSON` resources, including packed host copies, so migrated instant fields state the Unix millisecond timestamp unit. i18n impact: runtime UI language packs and manifest runtime language resources were not changed; API documentation i18n resources were changed intentionally. Cache consistency impact: no cache key, invalidation, distributed coordination, or freshness model changed. Data-permission impact: no data access scope was widened or changed; existing list/detail/query paths only project response field types. Runtime API impact: existing public response field types for instant fields changed from formatted/time-object-like values to numeric millisecond timestamps. Frontend page impact: system config/file/job/job-group/job-log/menu/message/plugin/role/role-auth/user/profile/notification pages and affected source-plugin pages were reviewed and updated where they display migrated fields. Development script impact: no static scanning tool, build script, test script, or default developer command was added or modified.
- 2026-05-18: Verification passed for FB-1. Commands: `cd apps/lina-core && go test ./pkg/apitime ./internal/controller/... ./internal/service/menu ./internal/service/role ./internal/service/sysinfo ./internal/service/plugin/internal/runtime -count=1`; `cd apps/lina-core && go test ./internal/cmd -count=1`; from `apps/lina-plugins`, `GOWORK=off go test lina-plugin-content-notice/backend/... -count=1`, `lina-plugin-monitor-loginlog/backend/...`, `lina-plugin-monitor-operlog/backend/...`, `lina-plugin-monitor-online/backend/...`, `lina-plugin-monitor-server/backend/...`, `lina-plugin-multi-tenant/backend/...`, `lina-plugin-org-center/backend/...`, and `lina-plugin-demo-source/backend/...`; temporary Go workspace smoke for `lina-plugin-demo-dynamic/backend/...`; `pnpm -F @lina/web-antd run typecheck`; `openspec validate api-timestamp-response-contract --strict`; `openspec validate --all`; `git diff --check && git -C apps/lina-plugins diff --check`. Review searches found no public API DTO `time.Time`/`gtime.Time` exposure, no searched frontend instant fields still typed as `string`, and direct Vue interpolation of migrated fields now goes through `formatTimestamp`.
